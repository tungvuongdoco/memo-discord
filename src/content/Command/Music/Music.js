import React from 'react'
import * as userController from '../../../server/api/User/user.controller';

export default async function MusicCommand(message, args, dataUser) {
  const [dataMusic, setDataMusic] = React.useState({});

  
}